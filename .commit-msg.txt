feat: panic recovery, CPU governor control, Docker stats, temperature sensors (#99)

- Add LogPanicWithStack helper for stack-trace-enriched panic logging
- Add panic recovery to Docker, GPU, VM collectors (previously had none)
- Restructure Notification collector to per-iteration recovery
- Add top-level crash recovery to Alerting engine and Watchdog runner
- Wrap all Orchestrator goroutines with panic recovery
- Upgrade all existing recovery blocks to use LogPanicWithStack
- Add CPU scaling governor controller with shutdown restore
- Add /cpu/governor, /docker/stats, /temperatures REST endpoints
- Add set_cpu_governor, get_docker_stats, get_temperatures MCP tools
- Add Docker per-container CPU% via cgroup v2 delta calculation
- Add convenience fields: MemoryUsageMB, MemoryPercent, RAMUsedMB, RAMTotalMB
- Add full temperature sensor readings with sensor type classification
- Add ZFS ARC SizeMB and UsagePercent convenience fields
- Fix TotalMemoryLimit from max to sum in Docker aggregate stats
- Fix array.go panic recovery to return proper error via named returns
- Fix system.go motherboard temp to use max instead of last-writer-wins
- Fix sysfs_cpu.go to use errors.Is for proper wrapped error detection
- Add CPUGovernorRequest validate:required tag
- Regenerate Swagger docs with CPU tag and new endpoints
